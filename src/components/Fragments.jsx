// Fragments in React.
function Fragments() {
    // Fragments used for prevent unneccessary tag usage.
    return (
        <>
            <p>Multiple tags or components could placed in parent tag.</p>
            <p>Instead of parent tag we use fragment to reduce parent tag usage.</p>
        </>
    );
}

export default Fragments;